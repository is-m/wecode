/**
 * 对象工具
 */
define(["rt/util"],function(){
	
	/**
	 * 合并两个对象或者数组
	 * @param fstObj 第一个对象 
	 * @param scdObj 第二个对象 
	 * @return 合并后的对象
	 * @throws 
	 */
	var merge = function(fstObj,scdObj){
		
	}
	
	/**
	 * 将对象数组转换城树结构对象
	 * @param objArray 对象数组
	 * @return 树对象
	 */
	var toTreeObject = function(objArray){
		
	}
	
	return {
		merge:merge
	   ,toTreeObject:toTreeObject
	}
});